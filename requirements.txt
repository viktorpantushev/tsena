# Tsena has no pip dependencies — it uses Python's standard library only.
#
# Runtime requirements (install manually):
#
#   Python >= 3.9
#   Ollama  https://ollama.com/download
#
# Recommended minimum model (7B+ reliably follows structured formatting instructions):
#   ollama pull mistral:7b
#
# Optional smaller model (fast breakdown, but may not follow progress-marker format):
#   ollama pull qwen2.5:3b
#
# Cloud cost catalogue (referenced in expense_estimator.py LLM_MODELS, not run locally):
#   qwen2.5:7b, qwen2.5:14b, llama2:70b
